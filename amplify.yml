version: 1
frontend:
  phases:
    preBuild:
      commands:
        - npm ci
    build:
      commands:
        - npm run check
  artifacts:
    baseDirectory: dist/site
    files:
      - '**/*'
  cache:
    paths:
      - node_modules/**/*
